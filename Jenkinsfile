node{
  echo "Before checkout"
  stage("Checkout"){}
  echo "After checkout"
  
  echo "Before build"
  stage("Build"){}
  echo "After Build"
  
  echo "DIR"
  bat "DIR"
  echo "Geting git version"
  bat "git --version"
}
